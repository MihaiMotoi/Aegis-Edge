{
  "name": "aegis-edge-server",
  "version": "0.1.0",
  "description": "Reference server for Aegis Edge: persistent review queue, moderator auth, warning/suspension state, and append-only decision log.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "reset-admin-password": "node scripts/reset-admin-password.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
